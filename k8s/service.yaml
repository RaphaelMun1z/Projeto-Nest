apiVersion: v1
kind: Service
metadata:
  name: document-service
  namespace: document-service
  labels:
    app.kubernetes.io/name: document-service
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: document-service
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30080
